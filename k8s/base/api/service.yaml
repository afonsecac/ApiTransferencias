apiVersion: v1
kind: Service
metadata:
  name: api-transferencias
  namespace: api-transferencias
  labels:
    app: api-transferencias
    component: api
spec:
  type: ClusterIP
  selector:
    app: api-transferencias
    component: api
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
